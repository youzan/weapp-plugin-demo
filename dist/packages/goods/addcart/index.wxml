<doc-page title="加入购物车组件">
  <demo-item>
    <view class="demo-item__code" slot="code">
      <rich-text type="text" nodes="{{ nodes }}">
      </rich-text>
    </view>

    <view>
      <text class="demo-item__subtitle">展示效果：</text>
      <add-cart
        alias="26yzjsolo0nwy"
        app-id="{{ appId }}"
        shop-id="{{ shopId }}"
        open-id="{{ openId }}"
      >
        <button class="demo-item__cart-btn">多sku商品</button>
      </add-cart>

      <add-cart
        alias="3nk96iyk66xhe"
        app-id="{{ appId }}"
        shop-id="{{ shopId }}"
        open-id="{{ openId }}"
      >
        <button class="demo-item__cart-btn">无sku无留言商品快速加入购物车</button>
      </add-cart>

      <!-- <navigator
        target="miniProgram"
        open-type="navigate"
        app-id="wxf11c5910cb729a82"
        path="/pages/goods/cart/index"
        extra-data="{{ extraData }}"
      >
        jump111
      </navigator> -->
    </view>
  </demo-item>
</doc-page>
